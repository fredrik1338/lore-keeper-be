package database

const (
	// Firestore is the database type for Firestore
	Firestore = "firestore"
	// SQLite is the database type for SQLite
	SQLite = "sqlite"
	// PG is the database type for Postgres
	PG = "pg"
)
